<div>
  <h1>Restaurants</h1>
  <ul>
    <% @restaurants.each do |restaurant| %>
    <li><%= link_to "#{restaurant.name}", restaurant %></li>
    <% end %>
  </ul>
  <%= link_to "Add a new restaurant", restaurants_new_path, class: "btn btn-outline-primary" %>
</div>
